# Survey: benchmark-datasource

**Created**: 2026-08-16 | **Re-surveyed (audit)**: 2026-08-18 | **Baseline**: v0.12.0 @ 8dbf2ca (main)
Phase-A output — the single source of truth for code state. Every citation
below is pasted verbatim from grep/read/execution output in the session that
wrote it. This re-survey supersedes the 66882c2 (v0.11.0, pre-implementation)
edition: the spec landed 20/20 via PR #35, then 111 commits of drift (the
retrieval-quality and retrieval-quality-v2 campaigns + v0.12.0 release churn)
touched the same trees. All verify commands below were re-run at 8dbf2ca.

## Items

```
item FR-001: "version the synthetic T1 corpus via benchmarks/datasource/manifest.json + CI content-hash"
  evidence:   benchmarks/datasource/manifest.json (76 lines) — schema "cairn-bench-datasource-manifest", dataset_version "DS-v1", t1 block: seed 49374, complexity "medium", sizes [100, 300, 500, 1000, 5000], generator_git_sha "ad866b24d75547addedc783d95cc44677804c19b" (verified a real commit this session: `git log --oneline -1 ad866b24...` → "ad866b2 feat(bench): T001 datasource tree-hash + manifest helper (FR-001)"), per-size entries each carrying tree_hash (64 hex) + counts {files, lines, bytes} (e.g. size 300: files 301, lines 63900, bytes 1718400, tree_hash "65e3df398b96f2cd...").
              Machinery: src/cairn/bench/datasource.py (562 lines) — `tree_hash(root, *, include_git_dir_marker=False)` (:171) the path-order-independent sorted-manifest digest ("<mode> <relpath>\0<sha256(content)>" entries sorted by relpath); `NOISE_DIR_NAMES = frozenset({"__pycache__", ".ruff_cache", ".mypy_cache", ".pytest_cache"})` (:128) pruned from every walk (added during DS-v2 drift, ddb6531); `validate_manifest` (:247) enforces REQUIRED_T1_KEYS/REQUIRED_ENTRY_KEYS/REQUIRED_COUNT_KEYS; `load_manifest`/`save_manifest` (byte-stable JSON).
              Validator: scripts/verify_datasource.py (487 lines) — regenerates the corpus per manifest recipe via `generate_corpus(root/size_<n>, size, complexity, seed)` and asserts tree_hash + counts; exit contract 0/1 drift/2 unusable-manifest/3 budget.
              CI: .github/workflows/ci.yml bench job step "Verify benchmark datasource (T1 content pin)" → `python scripts/verify_datasource.py --size 300` (a real gate — not continue-on-error; the job comment: "the one deliberate exception to the continue-on-error rule").
  status:     DONE
  verify:     Ran `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python scripts/verify_datasource.py --size 300` (the exact CI invocation) → "size 300: OK hash 65e3df398b96... counts files=301 lines=63900 bytes=1718400" + "OK: 1/1 size(s) match the pinned tree-hash and counts (FR-001/AC2)" + 3/3 budgets OK, EXIT=0. PASSED. (Sizes other than 300 not regenerated this session — CI cost control matches; full-size verify is the same code path.)
  gap:        none. (Note: CI verifies only size 300 per run, by design — "the full 5000 pin would burn CI minutes for no extra signal here", ci.yml comment.)
```

```
item FR-002: "vendor a ≤3MB real snapshot with provenance + NOTICE + size guard"
  evidence:   benchmarks/datasource/t2/ exists: yarl/ snapshot (33 files, vendored) + provenance.json + ground_truth/ (see FR-003). t2/provenance.json: upstream_repo "https://github.com/aio-libs/yarl", upstream_commit "dddcb82e4aa9665e15b43d90ad370d7e54ac1fbb", license "Apache-2.0", license_file "yarl/LICENSE", export_date "2026-08-15", vendored_bytes 437498, export_notes documenting kept/excluded paths (D-002).
              NOTICE now 100 lines with a dedicated section at :85-100: "Vendored content (benchmark corpus)" / "yarl (https://github.com/aio-libs/yarl), Copyright 2016-2021, Andrew Svetlov and aio-libs team. Vendored at pinned commit ... under benchmarks/datasource/t2/yarl/".
              Size guard: scripts/verify_datasource.py BUDGETS (:102-106) — T2_BUDGET_KB 3072, DS2_BUDGET_KB 3072 (sibling rule added in drift, b20ece4), DATASOURCE_BUDGET_KB 5120; measured as byte sums (`tree_bytes`, __pycache__ excluded); checked on EVERY verify_datasource invocation including CI's.
              Smoke: tests/test_t2_snapshot.py `test_t2_snapshot_builds_and_answers_queries` — copies tree to tmp, adds .git marker on the copy, `build_graph`, asserts symbols > 500, `find_definition("URL")` returns yarl's URL class, `get_callers("URL")` non-empty. CI step "T2 smoke test (vendored snapshot build+query)" → `pytest tests/ -q -k t2` (real gate).
              Drift addition (out of this spec's scope): benchmarks/datasource/ds2/second-corpus/attrs-26.1.0/ vendored by the retrieval-quality-v2 campaign (T007, own provenance.json, MIT, 69 git-tracked files; on-disk .ruff_cache is untracked local noise — `git ls-files ... | grep .ruff_cache` → 0).
  status:     DONE
  verify:     Ran `pytest tests/ -q -k t2` (the exact CI invocation) → "6 passed, 1924 deselected in 5.47s". Budget lines from the FR-001 verify run: "budget benchmarks/datasource/t2 : OK 466.7 / 3072 KB", "budget benchmarks/datasource/ds2: OK 865.8 / 3072 KB", "budget benchmarks/datasource : OK 1334.4 / 5120 KB". PASSED.
  gap:        none against the shipped decision set. (Fact, not a defect: the vendored snapshot is Python-only; the FR's "multi-language" wording was resolved to a single-language pick by D-002 "T2 snapshot — yarl at a pinned commit" — tech-spec.md:273.)
```

```
item FR-003: "ground-truth set consumed by src/cairn/eval.py + validator"
  evidence:   benchmarks/datasource/t2/ground_truth/ holds queries.jsonl (82 lines) + expectations.tsv (235 lines). Re-counted this session (python json parse of queries.jsonl): L1 58 (callers 20, definition 18, flow 10, impact 10) + L5 24 — meets ≥50 L1 / ≥20 L5. Expectations: 234 total (L1 160 + L5 74) — matches quality.json ground_truth block ("queries": 82, "expectations": 234) and the validator run below. Kinds cover definition/callers/impact/flow as the FR lists.
              eval.py consumption surface (src/cairn/eval.py, 2083 lines at HEAD): `class Expectation` (:138), `class GradedQuery` (:149), `load_ground_truth(ground_truth_dir)` (:175), `match_rank(symbol_id, results, k=10)` (:1829, two-tier identity-first matcher), beside the pre-existing `load_eval_queries` (:112) / `run_evaluation` (:2011).
              Validator: scripts/verify_ground_truth.py (492 lines) — copies t2/yarl to a throwaway workspace, adds the .git marker on the copy, `build_graph` fresh, verifies every expectation via `match_rank(exp.symbol_id, pool, k=len(pool))` against the full symbol inventory (presence semantics; exit 0/1 stale/2 infra; never rewrites — D-010).
  status:     DONE
  verify:     Ran `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python scripts/verify_ground_truth.py` → "snapshot ... -> fresh build: repos=1 symbols=1066 edges=2432 parse_errors=0"; every kind bucket "OK" (L1 callers 63/63, definition 19/19, flow 40/40, impact 38/38, L5 knowledge 74/74); final line "OK: 234/234 expectations across 82 queries verified on a fresh build (FR-003/AC5, TC-021)", EXIT=0. PASSED. (Rationale-citing-snapshot per query is in the dataset's AUTHORING discipline; queries.jsonl carries the query text + kind the matcher consumes.)
  gap:        none for L1/L5 counts + validator. L5 expectations verify against the graph surface (no OKF bundle exists for t2 — recorded honestly as l5_surface: "none" in quality.json; DS-v1 L5 scores 0.0 by construction).
```

```
item FR-004: "stamp artifacts with dataset+cairn version+machine profile; baselines/DS-v1; --baseline resolution"
  evidence:   Stamping: src/cairn/bench/datasource.py `build_artifact_stamp(*, manifest_path=None, env=None, t3_entry=None)` (:531) → {"dataset": {...}, "cairn_version": __version__, "machine_profile": {arch, cpu, cpu_count, os, runner_class}}; `runner_class` (:454) classifies reference-local vs ci-<slug>; `machine_profile` (:471). Applied once per CLI invocation at cli/bench.py:276-280 (`from cairn.bench.datasource import build_artifact_stamp; stamp = build_artifact_stamp()`).
              Committed baselines: benchmarks/baselines/DS-v1/ — perf.json, scaling.json, agent.json, quality.json + README.md; each carries schema "cairn-bench-baseline/1" + dataset {name benchmark-datasource, version DS-v1, tree_hash 65e3df39..., identity_size 300} + cairn_version "0.11.0" + machine_profile {arch arm64, cpu arm, cpu_count 10, os macOS-26.5.2-arm64-arm-64bit, runner_class reference-local} (verified by reading all four this session; 0.11.0 is the mint-time version — D-010 immutability, not drift). Also present: benchmarks/baselines/DS-v1.1/quality.json (added by the retrieval-quality campaign, 0704ea9 — sibling version, not a DS-v1 edit).
              CLI: cli/bench.py `--baseline <DS-version>` resolves `benchmarks/baselines/<version>/<suite>.json` (`_resolve_baseline_file` :78; unknown version → exit 1); `_render_baseline_header` :114 (dataset-version header, TC-007); `_warn_machine_profile_mismatch` :143 (advisory warning naming each mismatched field with both values, TC-009/010/011; never changes the exit code — D-005). Minted by scripts/mint_baselines.py (refuses overwrite without --force).
              CI: bench job cold-start falls back to `--baseline DS-v1` when no rolling cache entry restores (ci.yml "Run bench" step: `BASE="--baseline DS-v1"`).
  status:     DONE
  verify:     Ran `.venv/bin/cairn bench --suite perf --baseline NO-SUCH-DS` → "✗ Unknown baseline dataset version 'NO-SUCH-DS': not found under .../benchmarks/baselines (available: DS-v1, DS-v1.1)", TRUE-EXIT=1. Ran end-to-end from /tmp: `.venv/bin/cairn bench --suite perf --n-files 60 --baseline DS-v1` → resolved /Users/tan.le/Projects/cairn/benchmarks/baselines/DS-v1/perf.json; rendered "dataset benchmark-datasource @ DS-v1 (tree 65e3df398b96...)" + "cairn 0.11.0 · runner-class reference-local · arch arm64 · cpus 10"; fired "! MACHINE-PROFILE MISMATCH -- timings are NOT comparable across machines" naming "cpu_count: baseline 10 vs current 8" and the os pair; rendered the 15%-threshold comparison table; TRUE-EXIT=2 (regressions past threshold — the documented exit contract, comparing a 60-file run vs a 300-file baseline; advisory in CI via continue-on-error). Ran `pytest tests/test_bench.py -q -k baseline` → "12 passed"; `pytest tests/test_bench_stamping.py -q` → "16 passed". PASSED.
  gap:        none.
```

```
item FR-005: "generate the retrieval-quality, perf, and scaling reference tables in docs/benchmarks.md between sentinels"
  evidence:   docs/benchmarks.md carries exactly three sentinel pairs (re-grepped this session): "<!-- cairn-bench-tables:quality start -->" :80 / end :89, perf start :196 / end :214, scaling start :238 / end :245. `grep -c "_fill_" docs/benchmarks.md` → 0 (all families filled). Re-counted rows between sentinels: quality 2 data rows (L1 58/0.4174/0.2862, L5 † 24/0.0000/0.0000) + 3 note/provenance lines citing "Source: DS-v1 baseline (benchmarks/baselines/DS-v1/quality.json) — runner class reference-local ..., minted 2026-08-16, cairn 0.11.0, embed local / BAAI/bge-m3"; perf 15 op rows (build.* ×6 incl. build.derived.closure 25284.88 ms, embed_all, explore, find_definition, get_callees, get_callers, impact_analysis, impact_analysis_wide, search_symbols, semantic_search); scaling 4 rows (100/500/1000/5000; 5000 → 150000 symbols, build 475.929 s, DB 618.92 MB).
              Generator: scripts/gen_benchmark_tables.py (375 lines, stdlib-only) — reads benchmarks/baselines/DS-v1/{quality,perf,scaling}.json, rewrites ONLY bytes between each family's sentinels, pinned cell formats/sorting (byte-idempotency contract TC-025), exit 1 on missing artifact/sentinel/key.
              CI hand-edit guard: bench job step "Verify docs tables regenerated (T018 hand-edit guard)" → `python scripts/gen_benchmark_tables.py` + `git diff --exit-code docs/benchmarks.md` (real gate).
  status:     DONE
  verify:     Ran `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python scripts/gen_benchmark_tables.py && git diff --exit-code docs/benchmarks.md` (the exact CI invocation) → "already current (families: quality, perf, scaling)" + byte-identical diff, EXIT=0; `git status --porcelain` clean after the run. PASSED.
  gap:        none. (Tables cite DS-v1 by design; the DS-v1.1 quality re-mint from the retrieval-quality campaign is a sibling version, not folded into these tables.)
```

```
item FR-006: "T3 manifest pinning external repos + local command"
  evidence:   manifest.json t3.entries (re-read this session): exactly 2 pins at 2 scale points — {name "home-assistant/core", url "https://github.com/home-assistant/core", commit "0308f01b295a8ecfef9938b67514aa1b7b95e5bc", scale_hint "~27k files (18.2k Python) -- mid scale point"} and {name "torvalds/linux", url "https://github.com/torvalds/linux", commit "3eb40771c00a8488fa6ed2cc1fe203477908bf38", scale_hint "~70k files (26.7k .c, 16.6k .h) -- extreme scale point"}. Schema enforced by validate_manifest REQUIRED_T3_ENTRY_KEYS (datasource.py:120).
              Local command: scripts/fetch_t3_corpus.py — `git clone --no-checkout` + `git checkout --detach <pin>` then exact `rev-parse HEAD` equality vs the manifest pin (exit 3 names entry/expected/found); cache OUTSIDE the repo (~/.cairn/bench-t3, D-009); --run-bench saves an artifact stamped with the manifest entry ("repo + commit + scale, verbatim from the manifest entry, only after a verified checkout"). Documented network-free-in-CI: docs/benchmarks.md:246-275 "T3 scale runs (local)" with the command block and pin-enforcement contract, plus "no T3 fetch step exists in ci.yml".
              CI absence re-verified: `grep -n "fetch_t3\|git clone" .github/workflows/ci.yml` → no matches (exit 1). `grep -rn "git clone" src/cairn --include="*.py"` stays 0 (the script lives under scripts/ by design).
  status:     DONE
  verify:     Ran `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python scripts/fetch_t3_corpus.py --list` (no network) → both entries listed with pins + scale hints, EXIT=0. Ran the CI-absence greps above → no matches. tests/test_fetch_t3_corpus.py in the FR-anchored run (below) → passed. PASSED. (No live clone was performed — multi-GB network fetches are maintainer-run by design; pin-enforcement path is covered by the test file.)
  gap:        none. (Pins are not exercised in CI by design — network-free CI is the constraint the FR itself states.)
```

## Supporting evidence (re-verified at 8dbf2ca)

FR-anchored test files (all run this session in one invocation:
`pytest tests/test_bench_datasource.py tests/test_verify_datasource.py tests/test_datasource_t2.py tests/test_gen_benchmark_tables.py tests/test_fetch_t3_corpus.py tests/test_verify_ground_truth.py tests/test_t2_snapshot.py -q`
→ "136 passed in 5.72s"). Baseline/stamping: tests/test_bench.py -k baseline → 12 passed; tests/test_bench_stamping.py → 16 passed. T2 smoke (CI shape): `pytest tests/ -q -k t2` → "6 passed, 1924 deselected in 5.47s".

Bench stack symbols at HEAD (src/cairn/bench/): corpus.py 104 lines (unchanged substrate: DEFAULT_SEED = 0xC0DE :19, generate_corpus :22, profiles :43-48, corpus_stats :99); datasource.py 562 lines (new since old survey: tree_hash :171, load/save_manifest :215/:234, validate_manifest :247, MANIFEST_SCHEMA/MANIFEST_VERSION :106-107, build_artifact_stamp :531, runner_class :454, machine_profile :471, default_manifest_path :413, default_baselines_root :432, NOISE_DIR_NAMES :128); report.py 170; perf_suite.py 248; scaling_suite.py 123; agent_suite.py 666; timing.py 127. Version: `cairn.__version__` → "0.12.0" (src/cairn/__init__.py:2; pyproject.toml).

CI bench job shape at HEAD (ci.yml:305+): 1) checkout; 2) setup-python 3.12; 3) install -e .; 4) "Restore rolling bench baseline" (actions/cache/restore@v4, path bench-baseline.json + bench-baseline.sha, key `bench-rolling-v1-${{ runner.os }}-${{ runner.arch }}-${{ github.run_id }}` always-miss-by-design, restore-keys prefix; SHA sidecar attribution — redesigned in drift, 4a2d214); 5) "Verify benchmark datasource (T1 content pin)" `verify_datasource.py --size 300` (gate); 6) "T2 smoke test" `pytest tests/ -q -k t2` (gate); 7) "Verify docs tables regenerated" `gen_benchmark_tables.py` + `git diff --exit-code` (gate); 8) "Run bench" (continue-on-error; rolling --compare else `--baseline DS-v1`); 9) "Mint rolling baseline (main only)" + save cache; 10) bench_compare.py advisory summary; 11) upload artifact; 12) advisory PR comment. New sibling CI job from drift (not this spec): "ds2-seal: Verify DS-v2 seal" → `python benchmarks/datasource/ds2/verify_dataset.py` ("558 expectations, both corpora").

Counts re-verified this session — t2 ground truth: 82 queries = 58 L1 (callers 20 / definition 18 / flow 10 / impact 10) + 24 L5; 234 expectations = 160 L1 + 74 L5. manifest t1: 5 declared sizes, seed 49374, generator ad866b2 (real commit). manifest t3: 2 entries. benchmarks/ tree: 166 files total. Budgets (byte-sum method): t2 466.7/3072 KB, ds2 865.8/3072 KB, datasource total 1334.4/5120 KB — all OK. docs tables: quality 2 rows + 3 notes, perf 15 rows, scaling 4 rows; 3 sentinel pairs; 0 "_fill_". NOTICE: 100 lines (yarl attribution :85-100). baselines/: DS-v1 4 artifacts + README (stamped cairn 0.11.0, immutable D-010); DS-v1.1 quality.json + README (sibling, retrieval-quality campaign).

Drift notes vs the 66882c2 survey (informational, all verified above): every previously-absent surface now exists and passes; additions by later campaigns sharing the tree — benchmarks/datasource/ds2/ (DS-v2 ground truth: 198 queries = 154 L1 + 44 L5, 559-line expectations.tsv; attrs-26.1.0 second corpus), DS2_BUDGET_KB sibling budget, ds2-seal CI job, NOISE_DIR_NAMES tree_hash pruning (ddb6531), DS-v1.1 quality baseline, rolling-baseline SHA attribution (4a2d214), and the v0.12.0 fixture-rot fix (2573fee — test fixture cairn_version now dynamic).

Claims NOT re-verified this session: none load-bearing. The old survey's two unknowns are now groundable or moot: the pathological-closure claim ("closure build 340 s at 1,000 files", docs/phases/benchmark-datasource/spec.md:22-23) does not appear in the committed DS-v1 artifacts, which instead record build.derived.closure 25284.88 ms (n-files 300, perf.json) and scaling build_s 17.808 s @ 1000 / 475.929 s @ 5000 (scaling.json); the rerank-gate calibration claim remains phase-doc narrative, no code pointer — unknown — verify (does not affect any FR status).
